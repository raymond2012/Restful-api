import requests
import json
import pytest
import datetime
import logging
import urllib

temp = {
    "filter": "filter-v",
    "offset": "offset-v",
    "offset_id": "offset_id-v"
}


def test():
    url = "/snaps?"
    return urllib.pars
    e.urlencode(temp)


print(test())
